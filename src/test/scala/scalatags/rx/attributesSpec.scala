package scalatags.rx

import rx._
import scalatags.JsDom.all._
import Ctx.Owner.Unsafe._

class attributesSpec extends UnitSpec {

  import atttributes._

  "AttrValue" should "bind RX" in (pending)
}